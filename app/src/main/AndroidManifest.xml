<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.carl.seg2105project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WelcomeActivity"
            android:windowSoftInputMode="adjustPan|adjustResize" />
        <activity android:name=".CreateAccount" />
        <activity android:name=".SerivceProviderInfoActivity" />
        <activity android:name=".ServiceProviderInfo" />
        <activity
            android:name=".service_providers"
            android:label="@string/title_activity_service_providers"></activity>
    </application>

</manifest>